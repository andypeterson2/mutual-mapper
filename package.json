{
  "name": "mutuals-mapper-userscript",
  "version": "0.1.0",
  "private": true,
  "description": "Tampermonkey userscript: map your X/Twitter mutuals network entirely in the browser",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "test:watch": "node --test --watch 'tests/**/*.test.js'",
    "build": "node build.js",
    "build:watch": "node --watch build.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^20.9.0",
    "jszip": "^3.10.1"
  },
  "engines": {
    "node": ">=22"
  }
}
